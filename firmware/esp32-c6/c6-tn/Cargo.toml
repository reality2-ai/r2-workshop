[package]
name        = "r2-tn-node-c6"
version     = "0.3.0"
edition     = "2021"
description = "r2-workshop TRUE-TN node firmware (ESP32-C6 / RISC-V, WiFi-UDP RouteEngine; FireBeetle-2-C6 / Beetle-C6). No LoRa (r2.hw.lora absent)."
license     = "MIT OR Apache-2.0"
publish     = false

[[bin]]
name    = "r2-tn-node"
path    = "src/main.rs"
harness = false

[profile.release]
opt-level     = "s"
lto           = "fat"
codegen-units = 1
debug         = false
strip         = true

[profile.dev]
opt-level = "z"
debug     = true

[dependencies]
log         = "0.4"
anyhow      = "1"
esp-idf-svc = { version = "0.51", default-features = false, features = ["binstart", "std"] }

# TN-only: r2-esp WITHOUT `ble` (no NimBLE) + WITH `tn`. Same firmware as the
# esp32-s3 dfr1195 TN node; only the target/sdkconfig differ (RISC-V C6).
r2-esp  = { path = "../../../crates/r2-esp", default-features = false, features = ["tn"] }
r2-core = { path = "../../../crates/r2-core", default-features = false, features = ["alloc"] }
r2-wire = { path = "../../../crates/r2-wire", default-features = false, features = ["alloc"] }

[build-dependencies]
embuild = { version = "0.32", features = ["espidf"] }
